# Hetzner Cloud API token (NOT the old DNS-console token)

API_BASE="https://api.hetzner.cloud/v1"

# one line per zone:   zone.tld host1 host2 host3 ...
ZONES_FILE="/config/hcloud-zones.txt"

COMMENT="dyndns"

# default: A only
IPV6=0
BOTH=0

IPV4_URL="https://api.ipify.org"
IPV6_URL="https://api64.ipify.org"
